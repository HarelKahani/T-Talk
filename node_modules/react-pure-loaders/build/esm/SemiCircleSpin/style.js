
import { keyframes } from '@emotion/core';
import { globalAnimation } from '../mixins';
import { PRIMARY_COLOR } from '../variables';
var size = '35px';
var pos = 30;
var spinRotate = keyframes({
  '0%': {
    transform: 'rotate(0deg)'
  },
  '100%': {
    transform: 'rotate(360deg)'
  },
  '50%': {
    transform: 'rotate(180deg)'
  }
});
export default (function (color) {
  return {
    height: size,
    overflow: 'hidden',
    position: 'relative',
    width: size,
    ' > div': _objectSpread({}, globalAnimation(), {
      animation: "".concat(spinRotate, " 0.6s 0s infinite linear"),
      backgroundImage: "linear-gradient(transparent 0%, transparent ".concat(100 - pos, "%, ").concat(color || PRIMARY_COLOR, " ").concat(pos, "%, ").concat(color || PRIMARY_COLOR, " 100%)"),
      borderRadius: '100%',
      borderWidth: '0px',
      height: '100%',
      position: 'absolute',
      width: '100%'
    })
  };
});